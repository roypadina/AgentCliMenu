{
  "description": "acm-sessions: hand each session its Agent CLI Menu name, note, flags and due reminders at startup.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "command -v acm >/dev/null 2>&1 && acm hook session-start || exit 0",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
